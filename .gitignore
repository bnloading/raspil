# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Камера стрим-сервері (exe және пароль бар конфиг репозиторийге кірмесін)
camera-server/
.env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel
.env*
!.env.example

# Firebase local emulator cache/state
.firebase/
firestore-debug.log
ui-debug.log
firebase-debug.log
service-account.json

# Firestore backups — contain every customer record; must never be committed.
backups/

# Claude Code local tooling: the assistant's own settings, plus any skill packs installed into the
# project (ui-ux-pro-max ships ~4.6 MB of design CSVs). Developer tools, not app code — same
# reasoning as .vscode/ above.
.claude/
